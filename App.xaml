<?xml version="1.0" encoding="UTF-8" ?>
<Application xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="SimonApp1.App">

    <Application.Resources>
        <ResourceDictionary>

            <!-- 🎨 Цвета -->
            <Color x:Key="PrimaryBlue">#4A90FF</Color>
            <Color x:Key="PrimaryBlueLight">#66D1FF</Color>

            <Color x:Key="BackgroundLight">#FFFFFF</Color>
            <Color x:Key="BackgroundDark">#121212</Color>

            <Color x:Key="TextLight">#000000</Color>
            <Color x:Key="TextDark">#FFFFFF</Color>

            <!-- 🟦 Градиент для заголовка -->
            <LinearGradientBrush x:Key="TitleGradient" StartPoint="0,0" EndPoint="1,1">
                <GradientStop Color="{StaticResource PrimaryBlueLight}" Offset="0.1"/>
                <GradientStop Color="{StaticResource PrimaryBlue}" Offset="0.9"/>
            </LinearGradientBrush>

            <!-- ✨ Красивый заголовок -->
            <Style x:Key="TitleStyle" TargetType="Label">
                <Setter Property="FontSize" Value="48"/>
                <Setter Property="FontAttributes" Value="Bold"/>
                <Setter Property="HorizontalOptions" Value="Center"/>
                <Setter Property="TextColor" Value="{StaticResource TitleGradient}"/>
                <Setter Property="Shadow">
                    <Setter.Value>
                        <Shadow Brush="{StaticResource PrimaryBlue}"
                                Offset="0,4"
                                Radius="16"
                                Opacity="0.8"/>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- 🔹 Обычные кнопки (меню, настройки, старт и т.д.) -->
            <Style TargetType="Button" x:Key="MenuButtonStyle">
                <Setter Property="HeightRequest" Value="55"/>
                <Setter Property="CornerRadius" Value="16"/>
                <Setter Property="FontSize" Value="20"/>
                <Setter Property="FontAttributes" Value="Bold"/>
                <Setter Property="BackgroundColor" Value="#6C8BFF"/>
                <Setter Property="TextColor" Value="White"/>
                <Setter Property="Shadow">
                    <Setter.Value>
                        <Shadow Brush="Black"
                                Offset="0,4"
                                Radius="12"
                                Opacity="0.35"/>
                    </Setter.Value>
                </Setter>
            </Style>

            <!-- 🎮 Игровые кнопки -->
            <Style TargetType="Button" x:Key="GameButtonStyle">
                <Setter Property="CornerRadius" Value="40"/>
                <Setter Property="WidthRequest" Value="150"/>
                <Setter Property="HeightRequest" Value="150"/>
                <Setter Property="Shadow">
                    <Setter.Value>
                        <Shadow Brush="Black"
                                Offset="0,6"
                                Radius="18"
                                Opacity="0.45"/>
                    </Setter.Value>
                </Setter>
            </Style>

        </ResourceDictionary>
    </Application.Resources>
</Application>
